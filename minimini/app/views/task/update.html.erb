<h1><%= I18n.t('task.title.update') %></h1>
<p>Find me in app/views/task/create.html.erb</p>
<%= form_with do |form| %>
    <table border=1>
        <tr>
            <th width="150"><%= I18n.t('task.name') %></th>
            <td width="400"><%= form.text_field :title, size: "60", value:@task.name %></td>
        </div>
        </tr>
        <tr>
            <th width="150"><%= I18n.t('task.description') %></th>
            <td width="400"><%= form.text_area :description, size: "60x10" %></td>
        </tr>
        <tr>
            <th width="150"><%= I18n.t('task.status') %></th>
            <td width="400"><%= form.select :status, ["Not Started", "Work In Progress", "Done"] %></td>
        </tr>
        <tr>
            <th width="150"><%= I18n.t('task.label') %></th>
            <td width="400"><%= form.select :status, ["A: 重要度高", "B: 重要度低", "C: 緊急度高", "D: 緊急度低"] %></td>
        </tr>
        <tr>
            <th width="150"><%= I18n.t('task.due_date') %></th>
            <td width="400"><%= form.date_field :due_date %></td>
        </tr>
    </tr>
    </table>
<% end %>
<div>
<%= link_to I18n.t('task.title.list'), './list' %>
</div>
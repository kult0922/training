<%= form_for(@task) do |f| %>
  <p><%= f.label :name, 'Task Name' %>
     <%= f.text_field :name %></p>

  <p><%= f.label :description, 'Description' %>
     <%= f.text_field :description %></p>

  <p><%= f.submit t 'tasks.submit.button' %></p>
<% end %>

<%= render 'layouts/flash' %>
<div class="container">
  <div class="mt-3">
    <h1 class="text-center"><%= t('tasks.index.page_title')%></h1>
  </div>
  <div class="mt-5">
    <div>
      <table class="table table-bordered">
        <thead class="thead-light">
          <tr>
            <th scope="col"><%= t('tasks.index.status')%></th>
            <th scope="col"><%= t('tasks.index.task')%></th>
            <th scope="col"><%= t('tasks.index.end_date')%></th>
          </tr>
        </thead>
        <tbody>
          <% @tasks.each do |tasks| %>
          <tr>
            <td><%= tasks.status %></td>
            <td><a href="tasks/taskdetail/<%= tasks.id %>"><%= tasks.title %></a></td>
            <td><%= tasks.end_date == nil ? "-" : tasks.end_date.strftime("%Y/%m/%d") %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="text-right">
      <a class="btn btn-secondary" href="tasks/newtask" role="button"><%= t('tasks.index.submit_button')%></a>
    </div>
  </div>
</div>

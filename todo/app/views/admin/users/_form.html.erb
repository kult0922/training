<%= form_with(model: [:admin, user], local: true) do |form| %>
  <div class="form-row align-items-center mb-2">
    <div class="col-auto">
      <%= form.label :'ユーザ名' %>
      <%= form.text_field :account_name, class: 'form-control' %>
    </div>
    <div class="col-auto">
      <%= form.label :'パスワード' %>
      <%= form.password_field :password, class: 'form-control' %>
    </div>
  </div>
  <div class="custom-control custom-checkbox">
    <%= form.check_box :admin, class: 'custom-control-input' %>
    <label class="custom-control-label" for="user_admin"><%= I18n.t('users.new.admin') %></label>
  </div>
  <div class="row mb-2">
    <div class="col-auto">
      <%= form.submit class: 'btn btn-primary' %>
    </div>
    <div class="col-auto">
      <%= link_to I18n.t('projects.back'), admin_users_path, class: 'btn btn-secondary' %>
    </div>
  </div>
<% end %>

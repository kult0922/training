<div class="container">
  <h2><%= t '.page_title' %></h2>

  <div class="card">
    <div class="card-body">
      <p class="card-title"><%= @task.title %></p>
      <p class="card-text"> <%= @task.description %></p>
    </div>
  </div>

  <%= link_to t('.edit'), edit_task_path(@task), class: 'btn btn-outline-primary' %>
  <%= link_to t('.back'), root_path, class: 'btn btn-outline-primary' %>
</div>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to t('navbar.admin.title'), root_path, class: 'navbar-brand' %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to t('admin.users.index.title'), tasks_path, class: 'nav-link' %>
      </li>
    </ul>

    <ul class="navbar-nav">
      <% if logged_in? %>
        <li class="nav-item">
          <span class="nav-link text-light"><%= current_admin.name %><%= t 'navbar.Mr' %></span>
        </li>
      <% end %>
      <li class="nav-item">
        <%=
          if session[:admin_id]
            link_to t('navbar.logout'), admin_sessions_path, method: :delete, class: 'nav-link'
          else
            link_to t('navbar.login'), new_admin_sessions_path, class: 'nav-link'
          end
        %>
      </li>
    </ul>
  </div>
</nav>
